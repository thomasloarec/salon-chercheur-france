/**
 * Production canonical site URL.
 *
 * Used for building absolute links that must point at the live site
 * (e.g. an admin "view public page" link), NOT at the Lovable preview
 * origin returned by window.location.origin during development.
 */
export const PUBLIC_SITE_URL = 'https://lotexpo.com';
